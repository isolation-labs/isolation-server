{
  "name": "isogate",
  "version": "0.0.1",
  "description": "The light layer between Isolation Cloud and an OpenSandbox server: pairing, tunnel, view-auth doorman, secrets, workspace persistence.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": { "isogate": "dist/cli.js" },
  "main": "dist/index.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "http-proxy-3": "^1.20.5"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
